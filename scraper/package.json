{
  "name": "aus-beer-scraper",
  "private": true,
  "type": "module",
  "description": "CLI: cd scraper && npm install && npm run scrape -- <output.json> (optional: --out <path>). Use --html <file.html> to save the post-JS DOM for fixtures. First page only for Dan Murphy's /beer/all; set HEADFUL=1 to debug.",
  "scripts": {
    "scrape": "tsx src/cli.ts",
    "test": "tsx --test src/**/*.test.ts",
    "fixture:expected": "tsx scripts/write-fixture-expected.mts",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.51.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
